import { type PlanType } from './user.ts'

export const PLAN_NAME_CONFIG: Record<string, PlanType> = {
  'Sync basic Monthly': 'basic',
  'Sync basic yearly': 'basic',
  'Sync standard monthly': 'standard',
  'Sync standard yearly': 'standard',
}
